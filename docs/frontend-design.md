现在需要实现前端的部分，但是我只能描述我想使用的技术栈，以及大致描述我想要的页面样式，请你尽量来实现我的需求：

技术栈：

- 使用 Vue3 和 Element Plus，其中 Vue3 我想稍微在完成的过程中学习一下，所以你写入记录的部分请尽量详细明了
- 同时，你可以直接彻底删除当前的原生 HTML + CSS + JS 的设计。

前端设计：

- 整个颜色基调以白色 / 黑灰色为主色调
- 整个页面左侧约 70%~80% 的部分为电梯可视化部分，有 k 台电梯竖着放置，一台电梯上下贯穿整个屏幕，但是最顶上有一个类似于顶部标签的东西，是一个数字，注明当前电梯的序号，电梯左右之间排列好
- 具体的电梯由于楼层和电梯数均可调整，应当采用响应式布局，动态调整前端的展示
- 每一个楼层的展示：大致像是从竖着的方向上有 n 个排列紧密的方块（其中 n 是楼层数），一个方块内有两个按钮，按钮上分别显示「上箭头」和「下箭头」，表示发起一个 `hall` 请求；
- 点击某一台电梯的顶部标签，则会对该电梯的顶部标签进行高亮，然后可以在右侧的设置里面配置 `cabin` 请求；关于 `cabin` 请求的设置，应该只是需要在确定选中某个特定的电梯之后输入一个数字即可了，这里的设计我暂且不是很有头绪，请你自主设计；
- 电梯位置不再使用额外的“轿厢方块”平滑移动，而是直接高亮当前楼层方块。这样楼层网格更稳定，不会出现轿厢在格子之间卡顿移动的视觉问题。
  - `direction === "idle"`：当前楼层方块为绿色，表示电梯空闲停靠在这里。
  - `direction !== "idle" && doorOpen === false`：当前楼层方块为黄色，表示电梯正在移动。
  - `doorOpen === true`：当前楼层方块为红色，表示电梯在这一层开门等待。
  - 当前楼层方块右上角显示 `stops` 数量角标，移动时显示 ▲/▼ 方向标识。
- 右侧的配置部分则需要支持所有能够配置的设置项，这里包括：

  - 楼层数和电梯数，这里有限制：楼层数的范围是 [2, 40]，电梯数的范围是 [1, 10]，所以可以采用滑块组件的方式来调节，这个放在最上面
  - 显示当前的 tick 数，作为一个数字显示即可
  - 支持切换算法，采用下拉菜单的方式，同时名字不能是简单的 "scan" "look"，这只是后端用的代号，前端显示的时候可以改为 "SCAN 算法" "LOOK 算法" "最近优先算法" 等等
  - 最下面可以通过可视化显示一些当前的情况，或者直接搞一个小小的终端显示当前的日志也可以。
